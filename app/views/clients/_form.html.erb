<%= form_with(model: client) do |form| %>

  <% if client.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(client.errors.count, "Error") %> no permitió(eron) que este cliente fuera creado:</h2>

      <ul>
        <% client.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <div class="my-2">
      <%= form.label :name, "Nombre cliente", class: "col-12" %>
      <br>
      <%= form.text_field :name, required: true, placeholder: "Nombre del cliente", class: "ps-2 col-6 border rounded-2", style:"height: 40px" %>
    </div>
    <div class="my-2">
      <%= form.label :age, "Edad cliente", class: "col-12" %>
      <br>
      <%= form.text_field :age, required: true, placeholder: "Edad", class: "ps-2 col-1 border rounded-2", style:"height: 40px" %>
    </div>
    <br>
    <div>
      <button type="submit" class="col-4 col-md-1 p-2 mr-2 btn btn-primary border">Submit</button>
    </div>
  </div>

<% end %>
